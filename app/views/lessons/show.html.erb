<p>
	<strong>Title:</strong>
	<%= @lesson.title %>
</p>
<p>
	<strong>Description:</strong>
	<%= @lesson.description %>
	
</p>	
<p>
	Nr of words: <%= @lesson.words_qty%>
</p>

<h2>Translations</h2>
<%= render @lesson_translations%>

<h2>Add new word:</h2>
<%= render "translations/form"%>

<%= link_to 'back', lessons_path%>
| <%= link_to 'edit', edit_lesson_path(@lesson)%>